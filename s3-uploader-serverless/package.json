{
  "name": "s3-uploader-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "serverless dev",
    "deploy:dev": "bash -lc 'set -a && [ -f .env.dev ] && . .env.dev; set +a; serverless deploy --stage dev --region ap-southeast-1 --verbose'",
    "deploy:prod": "bash -lc 'set -a && [ -f .env.prod ] && . .env.prod; set +a; serverless deploy --stage prod --region ap-southeast-1 --verbose'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "dmzii",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.858.0",
    "@aws-sdk/cloudfront-signer": "^3.873.0",
    "@aws-sdk/s3-request-presigner": "^3.858.0",
    "bcryptjs": "^2.4.3",
    "ffmpeg-static": "^5.1.0",
    "fluent-ffmpeg": "^2.1.2",
    "jsonwebtoken": "^9.0.0",
    "sharp": "^0.32.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/aws-sdk": "^0.0.42",
    "@types/bcryptjs": "^2.4.2",
    "@types/jsonwebtoken": "^9.0.1",
    "serverless": "^4.4.18",
    "serverless-offline": "^14.4.0",
    "typescript": "^5.7.3"
  }
}
